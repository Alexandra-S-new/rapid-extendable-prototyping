// migrations/ (ADR-10): v1 hat keine Migrationskette — inkompatible
// schemaVersion wird strikt abgelehnt (persistence/load.ts). Dokumentierter,
// leerer Erweiterungspunkt für spätere Subtasks.
export {};
